
spring.application.name=subastas-api
server.port=8080

# --- MySQL (local) ---
spring.datasource.url=jdbc:mysql://localhost:3306/subastas?useSSL=false&serverTimezone=America/Argentina/Buenos_Aires&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=8Z|TmuL6k17V
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Azure SQL Database configuration
# NOTE: Replace the placeholder values below with your Azure SQL details.
# If you prefer to keep MySQL, the original MySQL config is commented out below for reference.
# Also ensure the Microsoft JDBC driver is present: add dependency `com.microsoft.sqlserver:mssql-jdbc` in `pom.xml` if needed.

# --- AZURE SQL (fill these) ---
# Replace <YOUR_SERVER> with your server name (without .database.windows.net)
# Replace <YOUR_DATABASE> with your database name
# Replace <YOUR_USERNAME> with your username (often user@server)
# Replace <YOUR_PASSWORD> with your password OR set env var DB_PASSWORD
# spring.datasource.url=jdbc:sqlserver://subastas-dai.database.windows.net:1433;database=Subastas_Da1;encrypt=true;trustServerCertificate=false;loginTimeout=30
# spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
# spring.datasource.username=adminsql@subastas-dai
# spring.datasource.password=${DB_PASSWORD:A12345678#}
# spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.format_sql=true

# JWT — en producción sobreescribir con la variable de entorno JWT_SECRET
jwt.secret=${JWT_SECRET:mi-clave-secreta-de-desarrollo-local-12345}
jwt.expiration=86400000

# Mail (configurar para producción)
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=
spring.mail.password=
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

# Multipart
spring.servlet.multipart.max-file-size=6MB
spring.servlet.multipart.max-request-size=50MB

# Uploads
app.uploads.base-path=${UPLOADS_PATH:uploads}

# Logging
logging.level.com.subastas=DEBUG
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web.socket=INFO
